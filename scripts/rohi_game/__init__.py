"""Rohi the capuchin — virtual pet that lives in the GitHub profile README.

Modules:
    render_rohi  — renders the animated SVG scene from the game state.
    rohi_engine  — game state machine (tick / actions), calls the renderer.
"""
